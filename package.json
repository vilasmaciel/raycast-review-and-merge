{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "review-and-merge",
  "title": "Review & Merge",
  "description": "Review and merge pull requests fast: list your PRs and review requests, approve, and merge or auto-merge in one click.",
  "icon": "extension-icon.png",
  "author": "fernando_vilas",
  "license": "MIT",
  "commands": [
    {
      "name": "my-pull-requests",
      "title": "My Pull Requests",
      "description": "Your open pull requests, grouped by repository.",
      "mode": "view"
    },
    {
      "name": "review-requests",
      "title": "Review Requests",
      "description": "Pull requests awaiting your review, plus ones closed without it.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "githubToken",
      "type": "password",
      "required": true,
      "title": "GitHub Personal Access Token",
      "description": "GitHub token with the `repo` scope and access to the repositories whose pull requests you want to see.",
      "placeholder": "ghp_…"
    },
    {
      "name": "organization",
      "type": "textfield",
      "required": false,
      "title": "Organization or Owner",
      "description": "Optional. Limit results to this GitHub org/owner. Leave empty to include all your pull requests across GitHub.",
      "placeholder": "my-org"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.19",
    "@raycast/utils": "^2.2.6"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.1.1",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "eslint": "^9.22.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "vitest run"
  }
}
